services:
  envsync_api:
    image: ghcr.io/envsync-cloud/envsync-api:latest
    env_file: [.env]
    restart: unless-stopped
    ports:
      - "${PORT:-4000}:4000"
    environment:
      NODE_ENV: production
      DATABASE_HOST: postgres
      REDIS_URL: redis://redis:6379
      S3_ENDPOINT: http://rustfs:9000
      S3_BUCKET_URL: http://rustfs:9000
      OPENFGA_API_URL: http://openfga:8090
      KEYCLOAK_URL: http://keycloak:8080
      MINIKMS_GRPC_ADDR: minikms:50051
      OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-agent:4318
    depends_on:
      - postgres
      - redis
      - rustfs
      - openfga
      - keycloak
      - minikms
      - clickstack
    networks: [envsync_network]

  envsync_init:
    image: ghcr.io/envsync-cloud/envsync-api:latest
    command: ["bun", "run", "scripts/prod-init.ts"]
    env_file: [.env]
    environment:
      DATABASE_HOST: postgres
      S3_ENDPOINT: http://rustfs:9000
      OPENFGA_API_URL: http://openfga:8090
      KEYCLOAK_URL: http://keycloak:8080
      MINIKMS_GRPC_ADDR: minikms:50051
      OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-agent:4318
    depends_on:
      - postgres
      - redis
      - rustfs
      - openfga
      - keycloak
      - minikms
      - clickstack
    restart: "no"
    networks: [envsync_network]

  postgres:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-envsync}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks: [envsync_network]

  redis:
    image: redis:7
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - redis_data:/data
    networks: [envsync_network]

  rustfs:
    image: rustfs/rustfs:latest
    restart: unless-stopped
    environment:
      RUSTFS_DATA_DIR: /data
      RUSTFS_ACCESS_KEY: ${RUSTFS_ACCESS_KEY:-rustfsadmin}
      RUSTFS_SECRET_KEY: ${RUSTFS_SECRET_KEY:-rustfsadmin}
      RUSTFS_CONSOLE_ENABLE: "true"
    volumes:
      - rustfs_data:/data
    networks: [envsync_network]

  keycloak_db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: keycloak
      POSTGRES_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD:-admin}
      POSTGRES_DB: keycloak
    volumes:
      - keycloak_db_data:/var/lib/postgresql/data
    networks: [envsync_network]

  keycloak:
    image: envsync-keycloak:${KEYCLOAK_IMAGE_TAG:-local}
    restart: unless-stopped
    entrypoint: ["/bin/sh", "-lc"]
    command:
      [
        "/opt/keycloak/bin/kc.sh import --dir /opt/keycloak/data/import --override true && exec /opt/keycloak/bin/kc.sh start-dev"
      ]
    ports:
      - "8080:8080"
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://keycloak_db:5432/keycloak
      KC_DB_USERNAME: keycloak
      KC_DB_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD:-admin}
      KC_BOOTSTRAP_ADMIN_USERNAME: ${KEYCLOAK_ADMIN_USER:-admin}
      KC_BOOTSTRAP_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD:-admin}
      KC_HTTP_ENABLED: "true"
      KC_PROXY_HEADERS: xforwarded
      KC_HOSTNAME_STRICT: "false"
    volumes:
      - ./docker/keycloak/realm-import:/opt/keycloak/data/import:ro
    depends_on:
      - keycloak_db
    networks: [envsync_network]

  openfga_db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: openfga
      POSTGRES_PASSWORD: ${OPENFGA_DB_PASSWORD:-openfga}
      POSTGRES_DB: openfga
    volumes:
      - openfga_db_data:/var/lib/postgresql/data
    networks: [envsync_network]

  openfga_migrate:
    image: openfga/openfga:v1.12.0
    command: migrate
    environment:
      OPENFGA_DATASTORE_ENGINE: postgres
      OPENFGA_DATASTORE_URI: postgres://openfga:${OPENFGA_DB_PASSWORD:-openfga}@openfga_db:5432/openfga?sslmode=disable
    depends_on:
      - openfga_db
    networks: [envsync_network]
    restart: "no"

  openfga:
    image: openfga/openfga:v1.12.0
    restart: unless-stopped
    command: run
    environment:
      OPENFGA_DATASTORE_ENGINE: postgres
      OPENFGA_DATASTORE_URI: postgres://openfga:${OPENFGA_DB_PASSWORD:-openfga}@openfga_db:5432/openfga?sslmode=disable
      OPENFGA_HTTP_ADDR: 0.0.0.0:8090
      OPENFGA_GRPC_ADDR: 0.0.0.0:8091
    depends_on:
      - openfga_migrate
    networks: [envsync_network]

  minikms_db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${MINIKMS_DB_USER:-postgres}
      POSTGRES_PASSWORD: ${MINIKMS_DB_PASSWORD:-postgres}
      POSTGRES_DB: minikms
    volumes:
      - minikms_db_data:/var/lib/postgresql/data
    networks: [envsync_network]

  minikms_migrate:
    image: postgres:17
    command: ["sh", "-c", "psql -h minikms_db -U ${MINIKMS_DB_USER:-postgres} -d minikms -f /migrations/001_initial_schema.sql && psql -h minikms_db -U ${MINIKMS_DB_USER:-postgres} -d minikms -f /migrations/002_vault_storage.sql && psql -h minikms_db -U ${MINIKMS_DB_USER:-postgres} -d minikms -f /migrations/003_escrow_recovery.sql"]
    environment:
      PGPASSWORD: ${MINIKMS_DB_PASSWORD:-postgres}
    volumes:
      - ./docker/minikms/migrations:/migrations:ro
    depends_on:
      - minikms_db
    networks: [envsync_network]
    restart: "no"

  minikms:
    image: ghcr.io/envsync-cloud/minikms:sha-9ffbf53
    restart: unless-stopped
    environment:
      MINIKMS_ROOT_KEY: ${MINIKMS_ROOT_KEY}
      MINIKMS_DB_URL: postgres://${MINIKMS_DB_USER:-postgres}:${MINIKMS_DB_PASSWORD:-postgres}@minikms_db:5432/minikms?sslmode=disable
      MINIKMS_REDIS_URL: redis://redis:6379
      MINIKMS_TLS_ENABLED: ${MINIKMS_TLS_ENABLED:-false}
    depends_on:
      - minikms_migrate
      - redis
    networks: [envsync_network]

  clickstack:
    image: clickhouse/clickstack-all-in-one:latest
    restart: unless-stopped
    ports:
      - "${CLICKSTACK_PORT:-8800}:8080"
      - "${CLICKSTACK_OTEL_GRPC_PORT:-4317}:4317"
      - "${CLICKSTACK_OTEL_HTTP_PORT:-4318}:4318"
    volumes:
      - clickstack_data:/data/db
      - clickstack_ch_data:/var/lib/clickhouse
      - clickstack_ch_logs:/var/log/clickhouse-server
    networks: [envsync_network]

  otel-agent:
    image: otel/opentelemetry-collector-contrib:0.111.0
    restart: unless-stopped
    command: ["--config=/etc/otel-agent.yaml"]
    volumes:
      - ./docker/otel-agent/otel-agent-config.yaml:/etc/otel-agent.yaml:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - /:/hostfs:ro
    depends_on:
      - clickstack
    networks: [envsync_network]

networks:
  envsync_network:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  rustfs_data:
  keycloak_db_data:
  openfga_db_data:
  minikms_db_data:
  clickstack_data:
  clickstack_ch_data:
  clickstack_ch_logs:
