// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RevokeCertArgs {
    pub serial_hex: String,
    pub org_id: String,
    pub reason: u32,
}

impl From<RevokeCertArgs> for super::Reducer {
    fn from(args: RevokeCertArgs) -> Self {
        Self::RevokeCert {
            serial_hex: args.serial_hex,
            org_id: args.org_id,
            reason: args.reason,
        }
    }
}

impl __sdk::InModule for RevokeCertArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `revoke_cert`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait revoke_cert {
    /// Request that the remote module invoke the reducer `revoke_cert` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`revoke_cert:revoke_cert_then`] to run a callback after the reducer completes.
    fn revoke_cert(&self, serial_hex: String, org_id: String, reason: u32) -> __sdk::Result<()> {
        self.revoke_cert_then(serial_hex, org_id, reason, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `revoke_cert` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn revoke_cert_then(
        &self,
        serial_hex: String,
        org_id: String,
        reason: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl revoke_cert for super::RemoteReducers {
    fn revoke_cert_then(
        &self,
        serial_hex: String,
        org_id: String,
        reason: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RevokeCertArgs {
                serial_hex,
                org_id,
                reason,
            },
            callback,
        )
    }
}
