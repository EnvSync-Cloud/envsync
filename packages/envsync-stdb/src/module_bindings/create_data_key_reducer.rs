// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CreateDataKeyArgs {
    pub root_key_hex: String,
    pub org_id: String,
    pub scope_id: String,
}

impl From<CreateDataKeyArgs> for super::Reducer {
    fn from(args: CreateDataKeyArgs) -> Self {
        Self::CreateDataKey {
            root_key_hex: args.root_key_hex,
            org_id: args.org_id,
            scope_id: args.scope_id,
        }
    }
}

impl __sdk::InModule for CreateDataKeyArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `create_data_key`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait create_data_key {
    /// Request that the remote module invoke the reducer `create_data_key` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`create_data_key:create_data_key_then`] to run a callback after the reducer completes.
    fn create_data_key(
        &self,
        root_key_hex: String,
        org_id: String,
        scope_id: String,
    ) -> __sdk::Result<()> {
        self.create_data_key_then(root_key_hex, org_id, scope_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `create_data_key` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn create_data_key_then(
        &self,
        root_key_hex: String,
        org_id: String,
        scope_id: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl create_data_key for super::RemoteReducers {
    fn create_data_key_then(
        &self,
        root_key_hex: String,
        org_id: String,
        scope_id: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CreateDataKeyArgs {
                root_key_hex,
                org_id,
                scope_id,
            },
            callback,
        )
    }
}
