// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct StoreGpgMaterialArgs {
    pub root_key_hex: String,
    pub org_id: String,
    pub fingerprint: String,
    pub armored_private_key: String,
    pub passphrase: String,
}

impl From<StoreGpgMaterialArgs> for super::Reducer {
    fn from(args: StoreGpgMaterialArgs) -> Self {
        Self::StoreGpgMaterial {
            root_key_hex: args.root_key_hex,
            org_id: args.org_id,
            fingerprint: args.fingerprint,
            armored_private_key: args.armored_private_key,
            passphrase: args.passphrase,
        }
    }
}

impl __sdk::InModule for StoreGpgMaterialArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `store_gpg_material`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait store_gpg_material {
    /// Request that the remote module invoke the reducer `store_gpg_material` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`store_gpg_material:store_gpg_material_then`] to run a callback after the reducer completes.
    fn store_gpg_material(
        &self,
        root_key_hex: String,
        org_id: String,
        fingerprint: String,
        armored_private_key: String,
        passphrase: String,
    ) -> __sdk::Result<()> {
        self.store_gpg_material_then(
            root_key_hex,
            org_id,
            fingerprint,
            armored_private_key,
            passphrase,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `store_gpg_material` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn store_gpg_material_then(
        &self,
        root_key_hex: String,
        org_id: String,
        fingerprint: String,
        armored_private_key: String,
        passphrase: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl store_gpg_material for super::RemoteReducers {
    fn store_gpg_material_then(
        &self,
        root_key_hex: String,
        org_id: String,
        fingerprint: String,
        armored_private_key: String,
        passphrase: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            StoreGpgMaterialArgs {
                root_key_hex,
                org_id,
                fingerprint,
                armored_private_key,
                passphrase,
            },
            callback,
        )
    }
}
