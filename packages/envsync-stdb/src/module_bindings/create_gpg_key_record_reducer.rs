// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CreateGpgKeyRecordArgs {
    pub uuid: String,
    pub org_id: String,
    pub user_id: String,
    pub name: String,
    pub email: String,
    pub fingerprint: String,
    pub key_id: String,
    pub algorithm: String,
    pub key_size: u32,
    pub public_key: String,
    pub private_key_ref: String,
    pub usage_flags: String,
    pub trust_level: String,
    pub expires_at: String,
    pub is_default: bool,
}

impl From<CreateGpgKeyRecordArgs> for super::Reducer {
    fn from(args: CreateGpgKeyRecordArgs) -> Self {
        Self::CreateGpgKeyRecord {
            uuid: args.uuid,
            org_id: args.org_id,
            user_id: args.user_id,
            name: args.name,
            email: args.email,
            fingerprint: args.fingerprint,
            key_id: args.key_id,
            algorithm: args.algorithm,
            key_size: args.key_size,
            public_key: args.public_key,
            private_key_ref: args.private_key_ref,
            usage_flags: args.usage_flags,
            trust_level: args.trust_level,
            expires_at: args.expires_at,
            is_default: args.is_default,
        }
    }
}

impl __sdk::InModule for CreateGpgKeyRecordArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `create_gpg_key_record`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait create_gpg_key_record {
    /// Request that the remote module invoke the reducer `create_gpg_key_record` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`create_gpg_key_record:create_gpg_key_record_then`] to run a callback after the reducer completes.
    fn create_gpg_key_record(
        &self,
        uuid: String,
        org_id: String,
        user_id: String,
        name: String,
        email: String,
        fingerprint: String,
        key_id: String,
        algorithm: String,
        key_size: u32,
        public_key: String,
        private_key_ref: String,
        usage_flags: String,
        trust_level: String,
        expires_at: String,
        is_default: bool,
    ) -> __sdk::Result<()> {
        self.create_gpg_key_record_then(
            uuid,
            org_id,
            user_id,
            name,
            email,
            fingerprint,
            key_id,
            algorithm,
            key_size,
            public_key,
            private_key_ref,
            usage_flags,
            trust_level,
            expires_at,
            is_default,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `create_gpg_key_record` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn create_gpg_key_record_then(
        &self,
        uuid: String,
        org_id: String,
        user_id: String,
        name: String,
        email: String,
        fingerprint: String,
        key_id: String,
        algorithm: String,
        key_size: u32,
        public_key: String,
        private_key_ref: String,
        usage_flags: String,
        trust_level: String,
        expires_at: String,
        is_default: bool,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl create_gpg_key_record for super::RemoteReducers {
    fn create_gpg_key_record_then(
        &self,
        uuid: String,
        org_id: String,
        user_id: String,
        name: String,
        email: String,
        fingerprint: String,
        key_id: String,
        algorithm: String,
        key_size: u32,
        public_key: String,
        private_key_ref: String,
        usage_flags: String,
        trust_level: String,
        expires_at: String,
        is_default: bool,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CreateGpgKeyRecordArgs {
                uuid,
                org_id,
                user_id,
                name,
                email,
                fingerprint,
                key_id,
                algorithm,
                key_size,
                public_key,
                private_key_ref,
                usage_flags,
                trust_level,
                expires_at,
                is_default,
            },
            callback,
        )
    }
}
