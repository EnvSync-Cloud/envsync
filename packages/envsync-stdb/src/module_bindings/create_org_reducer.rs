// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CreateOrgArgs {
    pub uuid: String,
    pub name: String,
    pub slug: String,
    pub logo_url: String,
    pub size: String,
    pub website: String,
    pub metadata: String,
}

impl From<CreateOrgArgs> for super::Reducer {
    fn from(args: CreateOrgArgs) -> Self {
        Self::CreateOrg {
            uuid: args.uuid,
            name: args.name,
            slug: args.slug,
            logo_url: args.logo_url,
            size: args.size,
            website: args.website,
            metadata: args.metadata,
        }
    }
}

impl __sdk::InModule for CreateOrgArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `create_org`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait create_org {
    /// Request that the remote module invoke the reducer `create_org` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`create_org:create_org_then`] to run a callback after the reducer completes.
    fn create_org(
        &self,
        uuid: String,
        name: String,
        slug: String,
        logo_url: String,
        size: String,
        website: String,
        metadata: String,
    ) -> __sdk::Result<()> {
        self.create_org_then(
            uuid,
            name,
            slug,
            logo_url,
            size,
            website,
            metadata,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `create_org` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn create_org_then(
        &self,
        uuid: String,
        name: String,
        slug: String,
        logo_url: String,
        size: String,
        website: String,
        metadata: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl create_org for super::RemoteReducers {
    fn create_org_then(
        &self,
        uuid: String,
        name: String,
        slug: String,
        logo_url: String,
        size: String,
        website: String,
        metadata: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CreateOrgArgs {
                uuid,
                name,
                slug,
                logo_url,
                size,
                website,
                metadata,
            },
            callback,
        )
    }
}
