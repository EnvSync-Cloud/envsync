// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DeleteEnvArgs {
    pub org_id: String,
    pub app_id: String,
    pub env_type_id: String,
    pub key: String,
}

impl From<DeleteEnvArgs> for super::Reducer {
    fn from(args: DeleteEnvArgs) -> Self {
        Self::DeleteEnv {
            org_id: args.org_id,
            app_id: args.app_id,
            env_type_id: args.env_type_id,
            key: args.key,
        }
    }
}

impl __sdk::InModule for DeleteEnvArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `delete_env`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait delete_env {
    /// Request that the remote module invoke the reducer `delete_env` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`delete_env:delete_env_then`] to run a callback after the reducer completes.
    fn delete_env(
        &self,
        org_id: String,
        app_id: String,
        env_type_id: String,
        key: String,
    ) -> __sdk::Result<()> {
        self.delete_env_then(org_id, app_id, env_type_id, key, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `delete_env` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn delete_env_then(
        &self,
        org_id: String,
        app_id: String,
        env_type_id: String,
        key: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl delete_env for super::RemoteReducers {
    fn delete_env_then(
        &self,
        org_id: String,
        app_id: String,
        env_type_id: String,
        key: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            DeleteEnvArgs {
                org_id,
                app_id,
                env_type_id,
                key,
            },
            callback,
        )
    }
}
