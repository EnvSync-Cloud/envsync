// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct WriteAuthTuplesArgs {
    pub tuples_json: String,
}

impl From<WriteAuthTuplesArgs> for super::Reducer {
    fn from(args: WriteAuthTuplesArgs) -> Self {
        Self::WriteAuthTuples {
            tuples_json: args.tuples_json,
        }
    }
}

impl __sdk::InModule for WriteAuthTuplesArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `write_auth_tuples`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait write_auth_tuples {
    /// Request that the remote module invoke the reducer `write_auth_tuples` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`write_auth_tuples:write_auth_tuples_then`] to run a callback after the reducer completes.
    fn write_auth_tuples(&self, tuples_json: String) -> __sdk::Result<()> {
        self.write_auth_tuples_then(tuples_json, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `write_auth_tuples` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn write_auth_tuples_then(
        &self,
        tuples_json: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl write_auth_tuples for super::RemoteReducers {
    fn write_auth_tuples_then(
        &self,
        tuples_json: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(WriteAuthTuplesArgs { tuples_json }, callback)
    }
}
