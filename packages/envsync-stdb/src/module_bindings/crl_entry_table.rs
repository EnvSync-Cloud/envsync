// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::crl_entry_type::CrlEntry;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `crl_entry`.
///
/// Obtain a handle from the [`CrlEntryTableAccess::crl_entry`] method on [`super::RemoteTables`],
/// like `ctx.db.crl_entry()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.crl_entry().on_insert(...)`.
pub struct CrlEntryTableHandle<'ctx> {
    imp: __sdk::TableHandle<CrlEntry>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `crl_entry`.
///
/// Implemented for [`super::RemoteTables`].
pub trait CrlEntryTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`CrlEntryTableHandle`], which mediates access to the table `crl_entry`.
    fn crl_entry(&self) -> CrlEntryTableHandle<'_>;
}

impl CrlEntryTableAccess for super::RemoteTables {
    fn crl_entry(&self) -> CrlEntryTableHandle<'_> {
        CrlEntryTableHandle {
            imp: self.imp.get_table::<CrlEntry>("crl_entry"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct CrlEntryInsertCallbackId(__sdk::CallbackId);
pub struct CrlEntryDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for CrlEntryTableHandle<'ctx> {
    type Row = CrlEntry;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = CrlEntry> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = CrlEntryInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> CrlEntryInsertCallbackId {
        CrlEntryInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: CrlEntryInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = CrlEntryDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> CrlEntryDeleteCallbackId {
        CrlEntryDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: CrlEntryDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct CrlEntryUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for CrlEntryTableHandle<'ctx> {
    type UpdateCallbackId = CrlEntryUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> CrlEntryUpdateCallbackId {
        CrlEntryUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: CrlEntryUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `id` unique index on the table `crl_entry`,
/// which allows point queries on the field of the same name
/// via the [`CrlEntryIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.crl_entry().id().find(...)`.
pub struct CrlEntryIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<CrlEntry, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> CrlEntryTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `crl_entry`.
    pub fn id(&self) -> CrlEntryIdUnique<'ctx> {
        CrlEntryIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> CrlEntryIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<CrlEntry> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<CrlEntry>("crl_entry");
    _table.add_unique_constraint::<u64>("id", |row| &row.id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<CrlEntry>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<CrlEntry>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `CrlEntry`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait crl_entryQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `CrlEntry`.
    fn crl_entry(&self) -> __sdk::__query_builder::Table<CrlEntry>;
}

impl crl_entryQueryTableAccess for __sdk::QueryTableAccessor {
    fn crl_entry(&self) -> __sdk::__query_builder::Table<CrlEntry> {
        __sdk::__query_builder::Table::new("crl_entry")
    }
}
