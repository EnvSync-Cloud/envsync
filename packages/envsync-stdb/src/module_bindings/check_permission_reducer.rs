// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CheckPermissionArgs {
    pub request_id: String,
    pub user_id: String,
    pub relation: String,
    pub object_type: String,
    pub object_id: String,
}

impl From<CheckPermissionArgs> for super::Reducer {
    fn from(args: CheckPermissionArgs) -> Self {
        Self::CheckPermission {
            request_id: args.request_id,
            user_id: args.user_id,
            relation: args.relation,
            object_type: args.object_type,
            object_id: args.object_id,
        }
    }
}

impl __sdk::InModule for CheckPermissionArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `check_permission`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait check_permission {
    /// Request that the remote module invoke the reducer `check_permission` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`check_permission:check_permission_then`] to run a callback after the reducer completes.
    fn check_permission(
        &self,
        request_id: String,
        user_id: String,
        relation: String,
        object_type: String,
        object_id: String,
    ) -> __sdk::Result<()> {
        self.check_permission_then(
            request_id,
            user_id,
            relation,
            object_type,
            object_id,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `check_permission` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn check_permission_then(
        &self,
        request_id: String,
        user_id: String,
        relation: String,
        object_type: String,
        object_id: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl check_permission for super::RemoteReducers {
    fn check_permission_then(
        &self,
        request_id: String,
        user_id: String,
        relation: String,
        object_type: String,
        object_id: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CheckPermissionArgs {
                request_id,
                user_id,
                relation,
                object_type,
                object_id,
            },
            callback,
        )
    }
}
