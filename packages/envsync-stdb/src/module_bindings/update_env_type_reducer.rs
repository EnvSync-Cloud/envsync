// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdateEnvTypeArgs {
    pub uuid: String,
    pub name: String,
    pub is_protected: bool,
    pub color: String,
}

impl From<UpdateEnvTypeArgs> for super::Reducer {
    fn from(args: UpdateEnvTypeArgs) -> Self {
        Self::UpdateEnvType {
            uuid: args.uuid,
            name: args.name,
            is_protected: args.is_protected,
            color: args.color,
        }
    }
}

impl __sdk::InModule for UpdateEnvTypeArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_env_type`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_env_type {
    /// Request that the remote module invoke the reducer `update_env_type` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_env_type:update_env_type_then`] to run a callback after the reducer completes.
    fn update_env_type(
        &self,
        uuid: String,
        name: String,
        is_protected: bool,
        color: String,
    ) -> __sdk::Result<()> {
        self.update_env_type_then(uuid, name, is_protected, color, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `update_env_type` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_env_type_then(
        &self,
        uuid: String,
        name: String,
        is_protected: bool,
        color: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_env_type for super::RemoteReducers {
    fn update_env_type_then(
        &self,
        uuid: String,
        name: String,
        is_protected: bool,
        color: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdateEnvTypeArgs {
                uuid,
                name,
                is_protected,
                color,
            },
            callback,
        )
    }
}
