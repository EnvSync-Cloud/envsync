// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct GetEnvArgs {
    pub request_id: String,
    pub root_key_hex: String,
    pub org_id: String,
    pub app_id: String,
    pub env_type_id: String,
    pub key: String,
}

impl From<GetEnvArgs> for super::Reducer {
    fn from(args: GetEnvArgs) -> Self {
        Self::GetEnv {
            request_id: args.request_id,
            root_key_hex: args.root_key_hex,
            org_id: args.org_id,
            app_id: args.app_id,
            env_type_id: args.env_type_id,
            key: args.key,
        }
    }
}

impl __sdk::InModule for GetEnvArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `get_env`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait get_env {
    /// Request that the remote module invoke the reducer `get_env` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`get_env:get_env_then`] to run a callback after the reducer completes.
    fn get_env(
        &self,
        request_id: String,
        root_key_hex: String,
        org_id: String,
        app_id: String,
        env_type_id: String,
        key: String,
    ) -> __sdk::Result<()> {
        self.get_env_then(
            request_id,
            root_key_hex,
            org_id,
            app_id,
            env_type_id,
            key,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `get_env` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn get_env_then(
        &self,
        request_id: String,
        root_key_hex: String,
        org_id: String,
        app_id: String,
        env_type_id: String,
        key: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl get_env for super::RemoteReducers {
    fn get_env_then(
        &self,
        request_id: String,
        root_key_hex: String,
        org_id: String,
        app_id: String,
        env_type_id: String,
        key: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            GetEnvArgs {
                request_id,
                root_key_hex,
                org_id,
                app_id,
                env_type_id,
                key,
            },
            callback,
        )
    }
}
