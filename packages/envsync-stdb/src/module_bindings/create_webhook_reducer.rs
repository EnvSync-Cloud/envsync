// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CreateWebhookArgs {
    pub uuid: String,
    pub name: String,
    pub org_id: String,
    pub user_id: String,
    pub url: String,
    pub event_types: String,
    pub webhook_type: String,
    pub app_id: String,
    pub linked_to: String,
}

impl From<CreateWebhookArgs> for super::Reducer {
    fn from(args: CreateWebhookArgs) -> Self {
        Self::CreateWebhook {
            uuid: args.uuid,
            name: args.name,
            org_id: args.org_id,
            user_id: args.user_id,
            url: args.url,
            event_types: args.event_types,
            webhook_type: args.webhook_type,
            app_id: args.app_id,
            linked_to: args.linked_to,
        }
    }
}

impl __sdk::InModule for CreateWebhookArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `create_webhook`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait create_webhook {
    /// Request that the remote module invoke the reducer `create_webhook` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`create_webhook:create_webhook_then`] to run a callback after the reducer completes.
    fn create_webhook(
        &self,
        uuid: String,
        name: String,
        org_id: String,
        user_id: String,
        url: String,
        event_types: String,
        webhook_type: String,
        app_id: String,
        linked_to: String,
    ) -> __sdk::Result<()> {
        self.create_webhook_then(
            uuid,
            name,
            org_id,
            user_id,
            url,
            event_types,
            webhook_type,
            app_id,
            linked_to,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `create_webhook` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn create_webhook_then(
        &self,
        uuid: String,
        name: String,
        org_id: String,
        user_id: String,
        url: String,
        event_types: String,
        webhook_type: String,
        app_id: String,
        linked_to: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl create_webhook for super::RemoteReducers {
    fn create_webhook_then(
        &self,
        uuid: String,
        name: String,
        org_id: String,
        user_id: String,
        url: String,
        event_types: String,
        webhook_type: String,
        app_id: String,
        linked_to: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CreateWebhookArgs {
                uuid,
                name,
                org_id,
                user_id,
                url,
                event_types,
                webhook_type,
                app_id,
                linked_to,
            },
            callback,
        )
    }
}
