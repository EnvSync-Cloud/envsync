// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CreateSettingsArgs {
    pub uuid: String,
    pub user_id: String,
    pub email_notifications: bool,
    pub theme: String,
}

impl From<CreateSettingsArgs> for super::Reducer {
    fn from(args: CreateSettingsArgs) -> Self {
        Self::CreateSettings {
            uuid: args.uuid,
            user_id: args.user_id,
            email_notifications: args.email_notifications,
            theme: args.theme,
        }
    }
}

impl __sdk::InModule for CreateSettingsArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `create_settings`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait create_settings {
    /// Request that the remote module invoke the reducer `create_settings` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`create_settings:create_settings_then`] to run a callback after the reducer completes.
    fn create_settings(
        &self,
        uuid: String,
        user_id: String,
        email_notifications: bool,
        theme: String,
    ) -> __sdk::Result<()> {
        self.create_settings_then(uuid, user_id, email_notifications, theme, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `create_settings` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn create_settings_then(
        &self,
        uuid: String,
        user_id: String,
        email_notifications: bool,
        theme: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl create_settings for super::RemoteReducers {
    fn create_settings_then(
        &self,
        uuid: String,
        user_id: String,
        email_notifications: bool,
        theme: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CreateSettingsArgs {
                uuid,
                user_id,
                email_notifications,
                theme,
            },
            callback,
        )
    }
}
