// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CreateAppWithAuthArgs {
    pub uuid: String,
    pub name: String,
    pub org_id: String,
    pub description: String,
    pub enable_secrets: bool,
    pub is_managed_secret: bool,
    pub public_key: String,
    pub private_key: String,
    pub metadata: String,
}

impl From<CreateAppWithAuthArgs> for super::Reducer {
    fn from(args: CreateAppWithAuthArgs) -> Self {
        Self::CreateAppWithAuth {
            uuid: args.uuid,
            name: args.name,
            org_id: args.org_id,
            description: args.description,
            enable_secrets: args.enable_secrets,
            is_managed_secret: args.is_managed_secret,
            public_key: args.public_key,
            private_key: args.private_key,
            metadata: args.metadata,
        }
    }
}

impl __sdk::InModule for CreateAppWithAuthArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `create_app_with_auth`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait create_app_with_auth {
    /// Request that the remote module invoke the reducer `create_app_with_auth` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`create_app_with_auth:create_app_with_auth_then`] to run a callback after the reducer completes.
    fn create_app_with_auth(
        &self,
        uuid: String,
        name: String,
        org_id: String,
        description: String,
        enable_secrets: bool,
        is_managed_secret: bool,
        public_key: String,
        private_key: String,
        metadata: String,
    ) -> __sdk::Result<()> {
        self.create_app_with_auth_then(
            uuid,
            name,
            org_id,
            description,
            enable_secrets,
            is_managed_secret,
            public_key,
            private_key,
            metadata,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `create_app_with_auth` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn create_app_with_auth_then(
        &self,
        uuid: String,
        name: String,
        org_id: String,
        description: String,
        enable_secrets: bool,
        is_managed_secret: bool,
        public_key: String,
        private_key: String,
        metadata: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl create_app_with_auth for super::RemoteReducers {
    fn create_app_with_auth_then(
        &self,
        uuid: String,
        name: String,
        org_id: String,
        description: String,
        enable_secrets: bool,
        is_managed_secret: bool,
        public_key: String,
        private_key: String,
        metadata: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CreateAppWithAuthArgs {
                uuid,
                name,
                org_id,
                description,
                enable_secrets,
                is_managed_secret,
                public_key,
                private_key,
                metadata,
            },
            callback,
        )
    }
}
