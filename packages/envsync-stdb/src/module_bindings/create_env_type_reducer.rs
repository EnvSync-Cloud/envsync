// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CreateEnvTypeArgs {
    pub uuid: String,
    pub org_id: String,
    pub name: String,
    pub app_id: String,
    pub is_default: bool,
    pub is_protected: bool,
    pub color: String,
}

impl From<CreateEnvTypeArgs> for super::Reducer {
    fn from(args: CreateEnvTypeArgs) -> Self {
        Self::CreateEnvType {
            uuid: args.uuid,
            org_id: args.org_id,
            name: args.name,
            app_id: args.app_id,
            is_default: args.is_default,
            is_protected: args.is_protected,
            color: args.color,
        }
    }
}

impl __sdk::InModule for CreateEnvTypeArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `create_env_type`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait create_env_type {
    /// Request that the remote module invoke the reducer `create_env_type` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`create_env_type:create_env_type_then`] to run a callback after the reducer completes.
    fn create_env_type(
        &self,
        uuid: String,
        org_id: String,
        name: String,
        app_id: String,
        is_default: bool,
        is_protected: bool,
        color: String,
    ) -> __sdk::Result<()> {
        self.create_env_type_then(
            uuid,
            org_id,
            name,
            app_id,
            is_default,
            is_protected,
            color,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `create_env_type` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn create_env_type_then(
        &self,
        uuid: String,
        org_id: String,
        name: String,
        app_id: String,
        is_default: bool,
        is_protected: bool,
        color: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl create_env_type for super::RemoteReducers {
    fn create_env_type_then(
        &self,
        uuid: String,
        org_id: String,
        name: String,
        app_id: String,
        is_default: bool,
        is_protected: bool,
        color: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CreateEnvTypeArgs {
                uuid,
                org_id,
                name,
                app_id,
                is_default,
                is_protected,
                color,
            },
            callback,
        )
    }
}
