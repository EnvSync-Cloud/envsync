// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct EncryptArgs {
    pub request_id: String,
    pub root_key_hex: String,
    pub org_id: String,
    pub scope_id: String,
    pub plaintext: String,
    pub aad: String,
}

impl From<EncryptArgs> for super::Reducer {
    fn from(args: EncryptArgs) -> Self {
        Self::Encrypt {
            request_id: args.request_id,
            root_key_hex: args.root_key_hex,
            org_id: args.org_id,
            scope_id: args.scope_id,
            plaintext: args.plaintext,
            aad: args.aad,
        }
    }
}

impl __sdk::InModule for EncryptArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `encrypt`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait encrypt {
    /// Request that the remote module invoke the reducer `encrypt` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`encrypt:encrypt_then`] to run a callback after the reducer completes.
    fn encrypt(
        &self,
        request_id: String,
        root_key_hex: String,
        org_id: String,
        scope_id: String,
        plaintext: String,
        aad: String,
    ) -> __sdk::Result<()> {
        self.encrypt_then(
            request_id,
            root_key_hex,
            org_id,
            scope_id,
            plaintext,
            aad,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `encrypt` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn encrypt_then(
        &self,
        request_id: String,
        root_key_hex: String,
        org_id: String,
        scope_id: String,
        plaintext: String,
        aad: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl encrypt for super::RemoteReducers {
    fn encrypt_then(
        &self,
        request_id: String,
        root_key_hex: String,
        org_id: String,
        scope_id: String,
        plaintext: String,
        aad: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            EncryptArgs {
                request_id,
                root_key_hex,
                org_id,
                scope_id,
                plaintext,
                aad,
            },
            callback,
        )
    }
}
