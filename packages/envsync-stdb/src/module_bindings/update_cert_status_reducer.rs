// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdateCertStatusArgs {
    pub uuid: String,
    pub status: String,
    pub revocation_reason: u32,
}

impl From<UpdateCertStatusArgs> for super::Reducer {
    fn from(args: UpdateCertStatusArgs) -> Self {
        Self::UpdateCertStatus {
            uuid: args.uuid,
            status: args.status,
            revocation_reason: args.revocation_reason,
        }
    }
}

impl __sdk::InModule for UpdateCertStatusArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_cert_status`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_cert_status {
    /// Request that the remote module invoke the reducer `update_cert_status` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_cert_status:update_cert_status_then`] to run a callback after the reducer completes.
    fn update_cert_status(
        &self,
        uuid: String,
        status: String,
        revocation_reason: u32,
    ) -> __sdk::Result<()> {
        self.update_cert_status_then(uuid, status, revocation_reason, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `update_cert_status` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_cert_status_then(
        &self,
        uuid: String,
        status: String,
        revocation_reason: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_cert_status for super::RemoteReducers {
    fn update_cert_status_then(
        &self,
        uuid: String,
        status: String,
        revocation_reason: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdateCertStatusArgs {
                uuid,
                status,
                revocation_reason,
            },
            callback,
        )
    }
}
