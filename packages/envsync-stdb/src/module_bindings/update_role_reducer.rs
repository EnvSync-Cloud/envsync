// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdateRoleArgs {
    pub uuid: String,
    pub name: String,
    pub is_admin: bool,
    pub is_master: bool,
    pub can_view: bool,
    pub can_edit: bool,
    pub have_billing_options: bool,
    pub have_api_access: bool,
    pub have_webhook_access: bool,
    pub have_gpg_access: bool,
    pub have_cert_access: bool,
    pub have_audit_access: bool,
    pub color: String,
}

impl From<UpdateRoleArgs> for super::Reducer {
    fn from(args: UpdateRoleArgs) -> Self {
        Self::UpdateRole {
            uuid: args.uuid,
            name: args.name,
            is_admin: args.is_admin,
            is_master: args.is_master,
            can_view: args.can_view,
            can_edit: args.can_edit,
            have_billing_options: args.have_billing_options,
            have_api_access: args.have_api_access,
            have_webhook_access: args.have_webhook_access,
            have_gpg_access: args.have_gpg_access,
            have_cert_access: args.have_cert_access,
            have_audit_access: args.have_audit_access,
            color: args.color,
        }
    }
}

impl __sdk::InModule for UpdateRoleArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_role`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_role {
    /// Request that the remote module invoke the reducer `update_role` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_role:update_role_then`] to run a callback after the reducer completes.
    fn update_role(
        &self,
        uuid: String,
        name: String,
        is_admin: bool,
        is_master: bool,
        can_view: bool,
        can_edit: bool,
        have_billing_options: bool,
        have_api_access: bool,
        have_webhook_access: bool,
        have_gpg_access: bool,
        have_cert_access: bool,
        have_audit_access: bool,
        color: String,
    ) -> __sdk::Result<()> {
        self.update_role_then(
            uuid,
            name,
            is_admin,
            is_master,
            can_view,
            can_edit,
            have_billing_options,
            have_api_access,
            have_webhook_access,
            have_gpg_access,
            have_cert_access,
            have_audit_access,
            color,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `update_role` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_role_then(
        &self,
        uuid: String,
        name: String,
        is_admin: bool,
        is_master: bool,
        can_view: bool,
        can_edit: bool,
        have_billing_options: bool,
        have_api_access: bool,
        have_webhook_access: bool,
        have_gpg_access: bool,
        have_cert_access: bool,
        have_audit_access: bool,
        color: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_role for super::RemoteReducers {
    fn update_role_then(
        &self,
        uuid: String,
        name: String,
        is_admin: bool,
        is_master: bool,
        can_view: bool,
        can_edit: bool,
        have_billing_options: bool,
        have_api_access: bool,
        have_webhook_access: bool,
        have_gpg_access: bool,
        have_cert_access: bool,
        have_audit_access: bool,
        color: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdateRoleArgs {
                uuid,
                name,
                is_admin,
                is_master,
                can_view,
                can_edit,
                have_billing_options,
                have_api_access,
                have_webhook_access,
                have_gpg_access,
                have_cert_access,
                have_audit_access,
                color,
            },
            callback,
        )
    }
}
