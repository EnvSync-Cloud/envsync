# Moved (Phase 5b)

Enterprise dashboard modules and pages live in:

`packages/envsync-enterprise-web`

Vite alias `@enterprise-modules` resolves that package (enterprise build)
or `enterprise-modules.stub.ts` (OSS build).
