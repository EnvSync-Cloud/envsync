# =============================================================================
# EnvSync Monorepo – single source of truth for environment variables
# Copy to .env and fill in values. All packages (api, web, landing) and
# docker-compose read from this file when run from the repo root.
# =============================================================================

# --- App (API) ---
NODE_ENV=development
PORT=4000
DB_LOGGING=false
DB_AUTO_MIGRATE=false
DATABASE_SSL=false

# --- Database (PostgreSQL) ---
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=envsync
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=envsync
POSTGRES_PORT=5432

# --- Dynamic Secret E2E Testing (separate Postgres for lease creation tests) ---
DYNAMIC_SECRET_PG_HOST=localhost
DYNAMIC_SECRET_PG_PORT=5433
DYNAMIC_SECRET_PG_USER=dynroot
DYNAMIC_SECRET_PG_PASSWORD=dynrootpass
DYNAMIC_SECRET_PG_DATABASE=dynamic_secrets

# --- S3 / RustFS ---
S3_BUCKET=envsync-bucket
S3_REGION=us-east-1
S3_ACCESS_KEY=rustfsadmin
S3_SECRET_KEY=rustfsadmin
S3_BUCKET_URL=http://localhost:19000/envsync-bucket
S3_ENDPOINT=http://localhost:19000
RUSTFS_PORT=19000
RUSTFS_HTTP_PORT=19001

# --- Redis ---
CACHE_ENV=production
REDIS_URL=redis://localhost:6379
REDIS_PORT=6379

# --- SMTP ---
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@localhost
# --- Mailpit ---
MAILPIT_PORT=8025
MAILPIT_SMTP_PORT=1025

# --- Keycloak (IdP; realm envsync on http://localhost:8080) ---
KEYCLOAK_URL=http://auth.lvh.me:8080
KEYCLOAK_REALM=envsync
KEYCLOAK_ADMIN_USER=admin
KEYCLOAK_ADMIN_PASSWORD=admin
KEYCLOAK_WEB_CLIENT_ID=envsync-web
KEYCLOAK_WEB_CLIENT_SECRET=
KEYCLOAK_CLI_CLIENT_ID=envsync-cli
KEYCLOAK_API_CLIENT_ID=envsync-api
KEYCLOAK_API_CLIENT_SECRET=
KEYCLOAK_ACCESS_TOKEN_LIFESPAN_SECONDS=3600
KEYCLOAK_SSO_SESSION_IDLE_TIMEOUT_SECONDS=604800
KEYCLOAK_SSO_SESSION_MAX_LIFESPAN_SECONDS=604800
KEYCLOAK_CLIENT_SESSION_IDLE_TIMEOUT_SECONDS=604800
KEYCLOAK_CLIENT_SESSION_MAX_LIFESPAN_SECONDS=604800
KEYCLOAK_WEB_REDIRECT_URI=http://api.lvh.me:4000/api/access/web/callback
KEYCLOAK_WEB_CALLBACK_URL=http://app.lvh.me:8001/auth/callback
KEYCLOAK_API_REDIRECT_URI=http://api.lvh.me:4000/api/access/api/callback

# --- URLs (API / Web / Landing) ---
LANDING_PAGE_URL=http://localhost:8002
DASHBOARD_URL=http://app.lvh.me:8001
MANAGEMENT_API_URL=http://localhost:4001
MANAGEMENT_DASHBOARD_URL=http://localhost:8003
MANAGEMENT_API_PORT=4001

# --- Edition / deployment mode ---
# Program plan (feat/the-big-update): hosted | selfhosted.
# Local/e2e multi-tenant SaaS-like dev: hosted. Self-host deploys set selfhosted.
ENVSYNC_DEPLOYMENT_MODE=hosted
ENVSYNC_EDITION=enterprise
ENVSYNC_MANAGEMENT_ENABLED=true
ENVSYNC_MANAGEMENT_WEB_ENABLED=true
ENVSYNC_LANDING_ENABLED=true
ENVSYNC_SINGLE_ORG_MODE=false
# Interim self-host EE multi-org cap for CLI only (Phase 1b/4). Web must never use this.
# ENVSYNC_MAX_ORGS=1
# Operator setup token for POST /api/setup/org (self-host only). Generated by deploy-cli at /etc/envsync/setup.token
# ENVSYNC_SETUP_TOKEN=
ENVSYNC_OBSERVABILITY_ENABLED=true
ENVSYNC_LICENSE_ENFORCEMENT=false
# lease | certificate | entitlement (Phase 4 JWT claims)
ENVSYNC_LICENSE_MODE=certificate
ENVSYNC_LICENSE_SERVER_URL=
ENVSYNC_LICENSE_KEY=
ENVSYNC_INSTALL_FINGERPRINT=
ENVSYNC_LICENSE_LEASE_TTL_SECONDS=300
# Phase 4: Coder-style entitlement JWT (signed by private license-server; verify with public key only)
# ENVSYNC_ENTITLEMENT_JWT=
# ENVSYNC_ENTITLEMENT_JWT_PATH=/etc/envsync/license/entitlement.jwt
# ENVSYNC_ENTITLEMENT_GRACE_SECONDS=259200
# ENVSYNC_LICENSE_PUBLIC_KEY_PEM=
# ENVSYNC_LICENSE_PUBLIC_KEY_PATH=
ENVSYNC_STACK_NAME=
ENVSYNC_RELEASE_VERSION=

# --- Frontend (Vite: only VITE_* are exposed to client) ---
VITE_API_BASE_URL=http://api.lvh.me:4000
VITE_MANAGEMENT_API_URL=http://localhost:4001
VITE_MANAGEMENT_DASHBOARD_URL=http://localhost:8003
VITE_ENVSYNC_EDITION=enterprise
VITE_ENVSYNC_DASHBOARD_VARIANT=enterprise
VITE_ENVSYNC_MANAGEMENT_ENABLED=true
VITE_ENVSYNC_MANAGEMENT_WEB_ENABLED=true
VITE_OTEL_ENDPOINT=http://localhost:4318
VITE_OTEL_SERVICE_NAME=envsync-web
VITE_OTEL_SDK_DISABLED=false
VITE_OTEL_TRACE_SAMPLE_RATE=1.0

# --- OpenFGA (authorization engine, playground http://localhost:8090/playground) ---
OPENFGA_API_URL=http://localhost:8090
OPENFGA_STORE_ID=
OPENFGA_MODEL_ID=
OPENFGA_HTTP_PORT=8090
OPENFGA_GRPC_PORT=8091

# --- miniKMS (envelope encryption) ---
MINIKMS_GRPC_ADDR=localhost:50051
MINIKMS_ROOT_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
MINIKMS_TLS_ENABLED=false

# --- OpenTelemetry / ClickStack (observability) ---
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:14318
OTEL_SERVICE_NAME=envsync-api
OTEL_SDK_DISABLED=false

# --- ClickStack / HyperDX (dashboard http://localhost:8800) ---
# Local dev note:
# - the browser/API OTLP token is generated by ClickStack at runtime
# - run `bun run clickstack:sync` after `docker compose up -d clickstack`
# - that command updates `.env.local` with the current VITE_HYPERDX_* values
CLICKSTACK_PORT=8800
CLICKSTACK_OTEL_GRPC_PORT=4317
CLICKSTACK_OTEL_HTTP_PORT=4318
OTEL_AGENT_OTLP_GRPC_PORT=14317
OTEL_AGENT_OTLP_HTTP_PORT=14318
VITE_HYPERDX_API_KEY=
VITE_HYPERDX_URL=http://localhost:4318
VITE_HYPERDX_DISABLED=false

# --- Docker / optional ---
# Mailpit (SMTP 1025, UI http://localhost:8025) – no extra env needed for local dev
